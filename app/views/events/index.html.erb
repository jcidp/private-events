<%= render "shared/header" %>

<h2>Events</h2>
<%= link_to "Create event", new_event_path %>
<br><br>
<p>Past events:</p>
<%= render "shared/events", events: @events.past, empty_state_message: "There aren't any past events :O" %>
<br>
<p>Upcoming events:</p>
<%= render "shared/events", events: @events.upcoming, empty_state_message: "There aren't any upcoming events, yet..." %>
