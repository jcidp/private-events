<h1>Events</h1>

<% if @events.exists? %>
  <% @events.each do |event| %>
    <h2><%= event.name %></h2>
    <p><%= event.date %></p>
    <p><%= event.location %></p>
  <% end %>
<% else %>
  <h2>There are no upcoming events...</h2>
<% end %>